# Google OAuth 2.0 Credentials
# =============================================================================
# Create at: https://console.cloud.google.com/apis/credentials
#
# Steps:
# 1. Create a new project (or select existing)
# 2. Enable the "Google Search Console API"
# 3. Go to Credentials > Create Credentials > OAuth 2.0 Client ID
# 4. Application type: "Desktop app" (for local use) or "Web application" (for HTTP server)
# 5. Download the JSON and copy the client_id and client_secret below
# =============================================================================

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Optional: Service Account Authentication
# =============================================================================
# Alternative to OAuth - useful for automated/server environments
# Download from: Google Cloud Console > IAM & Admin > Service Accounts
# Note: Service account must be added as a user in Google Search Console
# =============================================================================
# GSC_SERVICE_ACCOUNT_PATH=/absolute/path/to/service-account.json

# Optional: Token Storage Path
# =============================================================================
# Where to store the OAuth refresh token after authentication
# Default: ./token.json
# =============================================================================
# GSC_TOKEN_PATH=./token.json

# Optional: Default GSC Property
# =============================================================================
# Skip property selection if you only have one site
# Format: "sc-domain:example.com" or "https://www.example.com/"
# =============================================================================
# GSC_DEFAULT_SITE=sc-domain:example.com

# Optional: Logging
# =============================================================================
# Log level: debug | info | warn | error
# Default: info
# =============================================================================
# GSC_LOG_LEVEL=info

# HTTP Server Mode (optional)
# =============================================================================
# Only used when running: npm run start:http
# =============================================================================
# PORT=3000
